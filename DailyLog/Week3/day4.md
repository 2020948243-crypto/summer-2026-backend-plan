## Day 18 日志

### 今日完成

#### 算法刷题
- [ ] 题目1：
- [ ] 题目2：

#### 项目开发
- [ ] 任务1：后端Web进阶（SpringAOP）
            面向切面/特定方法编程
            在定义切面类的时候需要进行加上aspect这个注解，然后，再将这个类交给ioc容器进行管理，接下里就是pointcut，就是抽取，不过在抽取结束后需要使用的时候需要进行加上抽取出来的方法的名称，接下来就是两种切入点表达式，一个是原始的方式，就是一个个写包名，最多加上一些||的符号进行优化，我还是建议使用注解的形式进行引入切面类，就是先定义一个注解和一个aop，然后在aop的方法上面的注解上面配置上@After(@annotation("这里面写上你自己定义的注解的路径"))，然后就是通知的顺序，记住一句话，前置通知越小越先被执行，后置通知越大越先被执行
- [ ] 任务2：AOP与其他类的互动
            ThreadLocal:ThreadLocal为每个线程提供一份单独的存储空间，具有线程隔离的效果，不同的线程之间不会相互干扰。在同一个线程/同一个请求中，进行数据共享就可以使用 ThreadLocal

#### 底层学习
- [ ] MySQL：
- [ ] Redis：
- [ ] 计网/OS：

### 遇到的问题
1. 
2. 

### 解决方案
1. 
2. 

### 明日计划
1. 
2. 
3.